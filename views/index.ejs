<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <% messages.forEach(function(message) { %>
            <h3><%= message.user %></h3>
            <p><%= message.text %></p>
        <% }); %>
    </body>
</html>
